name: Line Contribution Report

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # để có toàn bộ history cho `git blame`

      - name: Tính số dòng đóng góp bằng git
        run: |
          echo "📊 **Báo cáo đóng góp dòng code** ($(date +%Y-%m-%d)):" > report.txt

          find . -type f -name '*.py' -o -name '*.js' -o -name '*.ts' -o -name '*.java' -o -name '*.cs' | while read file; do
            git blame --line-porcelain "$file" | grep "^author " >> authors.txt
          done

          sort authors.txt | uniq -c | sort -nr > authors_count.txt

          cat authors_count.txt | while read count author; do
            echo "- \`$author\`: $count dòng" >> report.txt
          done

          cat report.txt

      - name: Gửi báo cáo đến Discord
        run: |
          message=$(cat report.txt)
          payload=$(jq -Rn --arg msg "$message" '{content: $msg}')

          curl -H "Content-Type: application/json" \
               -X POST \
               -d "$payload" \
               $DISCORD_WEBHOOK
        env:
          DISCORD_WEBHOOK: https://discord.com/api/webhooks/1401911119290961962/PGaNzn5IWIiOgixqCCzMPfNsIoWaj77rTqh7W7MFfwc87ezxfhr-ZMTwWUQQ83hH1Y3E
